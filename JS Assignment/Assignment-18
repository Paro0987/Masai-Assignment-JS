function arrayInclude(arr, element)
{
  for(let i=0; i<arr.length;i++)
    {
      if(arr[i]===element)
      {
        return true;
      }
    }
  return false;
}
let result= arrayInclude([1,2,3,4,5],3);
console.log(result);
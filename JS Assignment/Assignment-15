function customIndex(arr, element)
{
  for(let i=0; i<arr.length; i++)
    {
      if(arr[i]===element)
      {
        return i;
      }
    }
}
let index= customIndex([1,2,3,4,5],3);
console.log(index);